{
  "name": "back",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "build:pipe": "npm run build --prefix ../pipe-game",
    "build:tower": "npm run build --prefix ../jeu-des-tours",
    "build:registration": "npm run build --prefix ../token-registration",
    "build:entry-menu": "npm run build --prefix ../entry-menu",
    "build:all": "npm run build:pipe && npm run build:tower && npm run build:registration && npm run build:entry-menu",
    "dev": "nodemon --watch src --watch fronts src/server.js",
    "lint": "eslint . --ext .js --fix",
    "mongo": "node db/inMemoryDb.js",
    "format": "prettier --write \"src/**/*.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb-memory-server": "^9.1.1",
    "mongoose": "^8.0.1",
    "nanoid": "^5.0.4",
    "which": "^4.0.0",
    "ws": "^8.15.0"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.0.0",
    "nodemon": "^3.0.2",
    "prettier": "3.1.0"
  }
}
